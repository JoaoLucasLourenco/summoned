import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./src/**/*.{js,tx,jsx,tsx}",
  ],
  theme: {
    extend: {
      
    },
  },
  plugins: [],
};
export default config;
